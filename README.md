# Платформа "ЧёПочём" - Система объявлений

Веб-приложение для размещения и управления объявлениями с полным функционалом CRUD, модерацией, системой отзывов и аналитикой.

## 📋 Содержание

- [Описание](#описание)
- [Технологии](#технологии)
- [Требования](#требования)
- [Установка](#установка)
- [Запуск](#запуск)
- [Архитектура](#архитектура)
- [Роли пользователей](#роли-пользователей)
- [API](#api)
- [База данных](#база-данных)
- [Безопасность](#безопасность)
- [Тестирование](#тестирование)

## 📝 Описание

Платформа "ЧёПочём" - это полнофункциональная система для размещения объявлений с поддержкой:
- Управления объявлениями (CRUD)
- Системы модерации
- Отзывов и репутации
- Аналитики и отчетности
- Импорта/экспорта данных
- Ролевой модели доступа (RBAC)
- Журнала аудита

## 🛠 Технологии

- **Backend**: Django 4.2.7, Django REST Framework
- **База данных (по умолчанию)**: SQLite (локальная разработка через Django ORM)
- **SQL‑скрипты БД**: 
  - PostgreSQL: `Skript che_pochem.sql` + файлы в папке `database/` (`functions.sql`, `stored_procedures.sql`, `triggers.sql`, `views.sql`)
  - MySQL: `chepochem_mysql.sql` (полная схема + базовые данные)
- **Frontend**: HTML, CSS, JavaScript, Bootstrap
- **Безопасность**: Django Auth, RBAC, хеширование паролей
- **Логирование**: Встроенное логирование Django + `logs/`

## 📦 Требования

- Python 3.8+
- pip
- virtualenv (рекомендуется)

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd CursachV2
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных (вариант через Django ORM, SQLite по умолчанию)

```bash
# Применение миграций (создаёт структуру БД из моделей Django)
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Инициализация базовых данных (роли, категории, админ)
python manage.py init_data
```

### 5. Настройка базы данных PostgreSQL (вариант с использованием SQL‑скриптов)

Этот вариант подходит, если вы хотите развернуть БД PostgreSQL с хранимыми функциями, процедурами и триггерами.

1. Создайте пустую БД PostgreSQL и пользователя (при необходимости).
2. Выполните основной скрипт структуры и данных (если используется):

```bash
psql -d your_database -f "Skript che_pochem.sql"
```

3. Примените дополнительные скрипты из папки `database/`:

```bash
psql -d your_database -f database/functions.sql
psql -d your_database -f database/stored_procedures.sql
psql -d your_database -f database/triggers.sql
psql -d your_database -f database/views.sql
```

4. В `chepochem_project/settings.py` укажите PostgreSQL как основную БД.

### 6. Настройка базы данных MySQL (полная схема и данные)

Для MySQL используется отдельный полный скрипт:

```bash
mysql -u your_user -p < "chepochem_mysql.sql"
```

Скрипт создаёт базу `chepochem_platform`, все таблицы, связи и тестовые данные (роли, пользователи, категории, примеры объявлений и отзывов).

## ▶️ Запуск

### Локальный запуск

```bash
python manage.py runserver
```

Приложение будет доступно по адресу: `http://127.0.0.1:8000/`

### Запуск через batch-файл (Windows)

```bash
run.bat
```

## 🏗 Архитектура

### Структура проекта

```
CursachV2/
├── chepochem_app/               # Основное Django‑приложение
│   ├── models.py                # Модели данных (User, Listing, Review и др.)
│   ├── views.py                 # Классические представления
│   ├── api_views.py             # DRF API endpoints
│   ├── server_logic_api.py      # Расширенная серверная логика для API
│   ├── serializers.py           # Сериализаторы DRF
│   ├── urls.py                  # URL маршруты
│   ├── forms.py                 # Формы
│   ├── django_orm_services.py   # Бизнес-логика и сервисы на ORM
│   ├── django_rbac_security.py  # RBAC, безопасность, аудит
│   ├── import_export.py         # Импорт/экспорт CSV
│   ├── activity_logger.py       # Логирование действий пользователей
│   ├── transaction_services.py  # Высокоуровневые транзакционные операции
│   └── management/
│       └── commands/
│           ├── init_data.py     # Инициализация ролей, категорий и администратора
│           └── backup_manager.py# Резервное копирование и восстановление
├── chepochem_project/           # Настройки проекта Django
├── database/                    # SQL‑скрипты (PostgreSQL‑совместимые)
│   ├── Skript che_pochem.sql    # (если используется как основной скрипт схемы/данных)
│   ├── views.sql                # SQL VIEW для отчётности
│   ├── stored_procedures.sql    # Хранимые процедуры (create/update/delete/modearte)
│   ├── functions.sql            # Функции (репутация, поиск, права и др.)
│   └── triggers.sql             # Триггеры (updated_at, репутация, уведомления и др.)
├── chepochem_mysql.sql          # Полный скрипт схемы и данных для MySQL
├── templates/                   # HTML‑шаблоны
├── static/                      # Статические файлы (CSS/JS)
├── media/                       # Медиа‑файлы (изображения объявлений и аватары)
└── logs/                        # Логи (activity.log, django.log, errors.log)

```

### Основные компоненты

1. **Модели данных** (`models.py`):
   - User, Role, UserProfile, UserReputation
   - Category, Listing, ListingImage
   - Review, UserFavorite, Report
   - Notification, UserStatistics
   - AuditLog, UserSettings

2. **API** (`api_views.py`, `server_logic_api.py`):
   - REST API для всех сущностей
   - Экспорт/импорт CSV
   - Аналитика и статистика

3. **Безопасность** (`django_rbac_security.py`):
   - RBAC (Role-Based Access Control)
   - Аудит действий
   - Хеширование паролей

## 👥 Роли пользователей

### 1. Администратор (admin)
- Полный доступ ко всем функциям
- Управление пользователями
- Модерация объявлений
- Просмотр аналитики
- Управление настройками системы

### 2. Модератор (moderator)
- Модерация объявлений
- Просмотр жалоб
- Просмотр аналитики
- Управление своими объявлениями

### 3. Пользователь (user)
- Создание и управление объявлениями
- Просмотр объявлений
- Оставление отзывов
- Добавление в избранное
- Подача жалоб

## 🔌 API

### Основные endpoints

- `GET /api/listings/` - Список объявлений
- `POST /api/listings/` - Создание объявления
- `GET /api/listings/{id}/` - Детали объявления
- `PUT /api/listings/{id}/` - Обновление объявления
- `DELETE /api/listings/{id}/` - Удаление объявления

- `GET /api/export/?type=listings` - Экспорт в CSV
- `POST /api/import/` - Импорт из CSV
- `GET /api/analytics/` - Данные для аналитики
- `GET /api/statistics/` - Общая статистика

Полная документация API доступна по адресу: `http://127.0.0.1:8000/api/`

## 🗄 База данных

### Структура

База данных содержит **12+ таблиц**:
- `roles` - Роли пользователей
- `users` - Пользователи
- `user_profiles` - Профили пользователей
- `user_reputation` - Репутация
- `categories` - Категории
- `listings` - Объявления
- `listing_images` - Изображения объявлений
- `reviews` - Отзывы
- `user_favorites` - Избранное
- `reports` - Жалобы
- `listing_moderation` - Модерация
- `notifications` - Уведомления
- `user_statistics` - Статистика
- `audit_log` - Журнал аудита
- `user_settings` - Настройки пользователя

### SQL VIEW

Созданы представления для отчетности (реализованы в `database/views.sql`, совместимо с SQLite и PostgreSQL):
- `v_listings_summary` - Сводная информация по объявлениям
- `v_users_statistics` - Статистика пользователей
- `v_categories_report` - Отчет по категориям
- `v_daily_activity` - Ежедневная активность
- `v_moderation_report` - Отчет по модерации
- `v_reviews_report` - Отчет по отзывам

### Хранимые процедуры и функции

Хранимые процедуры и функции реализованы в `database/stored_procedures.sql` и `database/functions.sql` (PostgreSQL‑совместимые):

- `create_listing(...)` - Создание объявления с валидацией и обновлением статистики
- `update_listing(...)` - Обновление объявления с проверкой прав
- `delete_listing(...)` - Удаление объявления с учётом ролей
- `create_review(...)` - Создание отзыва с перерасчётом репутации
- `moderate_listing(...)` - Модерация объявления и генерация уведомлений
- `update_user_reputation(...)` - Пересчёт репутации пользователя
- `calculate_user_statistics(...)` - Расчёт агрегированной статистики
- `search_listings(...)` - Поиск объявлений с фильтрацией и сортировкой
- `check_user_permission(...)` - Проверка прав доступа на уровне БД

### Триггеры

Триггеры реализованы в `database/triggers.sql` (PostgreSQL‑совместимые):

- Автоматическое обновление `updated_at` в основных таблицах
- Создание профиля, репутации и статистики при регистрации пользователя
- Обновление репутации при создании/изменении/удалении отзыва
- Обновление счетчиков избранного и просмотров
- Логирование критических изменений в `audit_log`
- Автоматическое создание уведомлений при модерации и отзывах
- Валидация данных объявлений на уровне БД
- Автоматическое истечение просроченных объявлений

## 🔒 Безопасность

### Реализованные меры

1. **RBAC** - Ролевая модель доступа
2. **Хеширование паролей** - Django PBKDF2
3. **Журнал аудита** - Логирование всех действий
4. **Валидация данных** - Серверная валидация
5. **CSRF защита** - Django CSRF middleware
6. **SQL инъекции** - Защита через ORM

### Резервное копирование

Для резервного копирования используется management‑команда `backup_manager` (`chepochem_app/management/commands/backup_manager.py`):

```bash
# Полная резервная копия (БД + media + ключевые настройки)
python manage.py backup_manager create-full

# Инкрементальная копия (для PostgreSQL, при наличии служебных таблиц)
python manage.py backup_manager create-incremental

# Восстановление из архива
python manage.py backup_manager restore --file=backups/full_backup_YYYYMMDD_HHMMSS.tar.gz

# Очистка старых резервных копий
python manage.py backup_manager cleanup --retention-days=30
```

## ⌨️ Горячие клавиши

- `Alt + Shift + K` - Поиск
- `Alt + Shift + N` - Новое объявление
- `Alt + Shift + F` - Избранное
- `Alt + P` - Профиль
- `Alt + H` - Главная
- `Alt + C` - Категории
- `Alt + Shift + C` - Мои объявления
- `Alt + Shift + R` - Отзывы
- `Alt + S` - Сохранить (в формах)
- `Ctrl + /` - Показать справку по горячим клавишам
- `/` - Фокус на поиск
- `Esc` - Закрыть модальное окно

## 📞 Контакты

Для вопросов и предложений создайте issue в репозитории.


